# 支持范围

这是一个业余时间维护的小工具，先把边界说清楚，省得互相浪费时间。

## 会处理

- `adi stats` / `adi doctor` 在任何环境下崩溃或报错
- `adi run` 打标失败、报告数字明显不对
- 脱敏漏掉了某种凭证格式 —— **这类问题优先级最高**，请直接说清是哪类格式（不要贴真实凭证）
- 文档写错了、说明和实际行为不一致

## 不处理

- 「能不能支持 X 工具」——除非你愿意提 PR
- LLM 打标结果和你的主观判断不一致：这属于模型判断差异，不是 bug。
  但如果**同一段会话多次运行结果差异巨大**（不是 ±1 那种），那是 bug，请报。
- 定制化报告样式 / 私有部署 / 商业支持

## 提 issue 前请做一件事

```bash
adi doctor --issue
```

把它的输出整段贴进 issue。它只包含操作系统、工具版本、会话计数和错误码，
**不含任何会话内容**——可以放心贴在公开仓库里。

没有这段输出的环境类 issue，多半只能靠猜，处理会很慢。

## 响应节奏

不承诺 SLA。安全与隐私类问题优先。
